# 17 Ağustos 2026 (b) — defterin 28 kayıtsız kalemi (N14)

**Tek cümlelik sonuç: 28'in 28'i kapandı. Kayıtsız sayı 0, basılı-değer ↔ alan uyuşmazlığı 0,
tanım sorunu 0; denetçi makaleye karşı artık çıkış kodu 0 veriyor. Bunun bir "yeşile boyama"
olmadığının kanıtı öz sınamada: bugün bağlanan sayılar da geri konup denendi ve 11 senaryonun
11'i yakalandı.** Yeni koşu yok, GPU yok: tamamı önbellek/CPU/metin.

Kapanış üç yoldan oldu ve üçünün oranı bu turun asıl sonucu: **3 sayı için kaynak ÜRETİLDİ,
20 sayı için var olan alan BULUNDU (üretici yazmaya gerek yoktu), 5 sayı türetilmiş nicelik
olarak pay/payda alan yoluyla KAYDEDİLDİ.** Yani 28 kalemin yalnız üçü gerçek bir boşluktu;
kalanı beyan eksikliğiydi. Bu, N13'ün kendi hükmünde iki düzeltme demek (§4).

| çıktı | ne |
|---|---|
| `paper_tables/number_ledger.{md,json}` | 617 alan bağı (N13: 590) |
| `paper_tables/derived_registry.{md,json}` | 16 türetilmiş nicelik (N13: 10) |
| `diagnostics/check_numbers.py` | denetçi — **çıkış 0**, ihlal yok |
| `diagnostics/check_numbers_selftest.py` | **11/11 yakalandı** (N13: 7/7) |
| `diagnostics/two_dataset_overlay.py` | **yeni**: havuzlanmış Pearson (`tab_pooled`'un `r` sütunu) |
| `diagnostics/table_diff_gate.py` | üç blok daha kapıya girdi (§6) |

---

## 1 — Sayım

| | N13 (dün) | N14 (bugün) |
|---|---|---|
| kapsamdaki sayı jetonu | 714 | **719** |
| bir artefakt alanına bağlı | 590 | **617** |
| türetilmiş (pay + payda alan yoluyla) | 10 | **16** |
| düzyazıda tek tek beyanlı bağ | 1 | **1** |
| "ölçüm değil" diye beyanlı (15 sınıf) | 88 | **90** |
| **kayıtsız** | **28** | **0** |
| basılı değer ↔ alan uyuşmazlığı | 0 | **0** |
| tanım sorunu (belirsiz/çürümüş/çift bağ) | 0 | **0** |

Jeton sayısı 714'ten 719'a çıktı çünkü makale tarafındaki `T^*` düzeltmesi alt yazıya beş yeni
sayı ekledi (`1.3406`, `1.3494`, `0.98`, `T{=}1` ve `Stage1` adındaki basamak). Beşi de bu turda
karşılandı.

## 2 — Üretilen kaynak: `tab_pooled`'un Pearson sütunu (3 sayı)

Karar uygulandı: **sütun kaldı, kaynağı üretildi.** `two_dataset_overlay.py`'ye `pearson()`
eklendi ve Spearman'ın **yanına**, aynı 14 noktayı toplayan **aynı döngüden** hesaplanıyor —
ikinci bir nokta kümesi ya da ikinci bir tanım getirmeden.

| checkpoint | yayımlı | ölçülen | 3 basamakta |
|---|---|---|---|
| @SWA | $+0.930$ | +0.9300833 | +0.930 ✓ |
| @best | $+0.970$ | +0.9701353 | +0.970 ✓ |
| @last | $+0.948$ | +0.9481784 | +0.948 ✓ |

**Yayımlı üç değer de birebir tutuyor** — düzeltme gerekmedi. İşaretli Pearson da yazıldı
(−0.791 / −0.866 / −0.835); makale onu basmıyor ama Spearman'ın işaretli sütunu var ve tek
eksenin kayıtlı olması yarım kayıt olurdu.

**Yan bulgu — depodaki artefakt kendi üreticisine göre bayattı.** `two_dataset_overlay.json`
yeniden üretildiğinde **42 `per_seed` bloğu** eklendi: bu alanlar 14 Ağustos'ta (B8) üreticiye
girmişti ama artefakt o günden beri yeniden koşulmamıştı. Anlamsal fark alındı: **eklenen 48 alan,
silinen 0, DEĞİŞEN 0.** Yani hiçbir ölçüm değeri kaymadı, eksik olan yalnız kayıttı. Kapı bunu
göremezdi çünkü kapı artefaktı kendi temel çizgisiyle karşılaştırır, üreticinin taze çıktısıyla
değil.

## 3 — Üretici yazmaya gerek olmayan 20 sayı

### 3a · `tab_selection_audit` FERPlus satırları (10 sayı) — artefakt zaten vardı

`selection_gain.audit_deltas` gerçekten yalnız RAF-DB kırılımı taşıyor. Ama **aynı estimand
FERPlus için de üretilmiş**: `selection_audit_inference.json` dört kontrastın (RAF-DB/FERPlus ×
best−last/best−SWA) hepsini aynı donmuş CSV'lerden ve aynı tanımla hesaplıyor. **Yeni üretici
yazılmadı**; ikinci bir tanım getirmek yerine var olan alan bağlandı.

İki artefaktın **örtüşen sekiz RAF-DB değeri bit düzeyinde aynı** (mean/sd, iki metrik, iki
kontrast — hepsi `True`). Yani tablonun RAF-DB satırlarının `selection_gain`'e, FERPlus
satırlarının `selection_audit_inference`'a bağlı olması bir tanım ayrışması değil; iki üretici
aynı sayıyı veriyor ve bu ölçüldü.

On değerin onu da yayımlı haliyle tutuyor (ör. +0.499526 → `+0.50`, 0.007447 → `0.0074`).

### 3b · Ölçüt sabiti değil, ÖLÇÜLEN oran: `tab_logitstd` alt yazısı (4 sayı)

| basılı | alan | ölçülen |
|---|---|---|
| $23\times$ "narrowest SWA comparison" | `noise_units.nine_cell_grid["swa\|primary"].ratio` | 23.4523 |
| $27\times$ (dokuz hücrenin medyanı) | `noise_units.summary.median` | 27.3441 |
| $52\times$ (ortalama) | `noise_units.summary.mean` | 51.6641 |
| $2.6\times$ (tüm-checkpoint tabanı) | `noise_units.summary.min` | 2.5568 |

@SWA'da üç hücre 114.3 / **23.5** / 213.0; "narrowest SWA" gerçekten primary hücresi. Özetin
"median $27\times$"i de aynı alana bağlandı — aynı sayının iki yerde basılması da bir bağdır.

### 3c · `tab_selection` dipnotunun iki sıra korelasyonu (2 sayı)

$\rho_s(\text{teacher acc.},\text{student acc.})=-0.50$ ve
$\rho_s(-\text{teacher ECE},\text{student acc.})=+1.00$ **artefaktta zaten hesaplanıyordu**:
`p4_teacher_selection.recipe_step3_ranking.spearman_{teacherACC,negTeacherECE}_vs_studentACC`
= −0.5 ve +1.0. N13'ün "artefaktı yok" hükmü yanlıştı; eksik olan beyandı.

### 3d · Özetin asimetri aralığı (2 sayı)

$1.8$–$2.0\times$ = `asymmetry_estimand.summary.interpolated_only.absolute.{min,max}`
= 1.7706 / 2.0443. İkisi de **alan**, türetme değil: artefaktın kendi özet bloğu, ve
"interpolated_only" seçimi tesadüfi değil — ekstrapole edilen dört karşılaştırma dışarıda.

### 3e · `tab_mechanisms` "G2G + adaptive $T$" satırı (2 sayı)

`T5_mechanisms["vae9182/g2g_kl+adaptive_t"].swa` **VAR**: `d_acc_mean` −0.065186 → `-0.07`,
`d_ece_mean` −0.0018300 → `-0.0018`, n=1, sd=0. N13 "T5 yalnız 3×7=21 tekil mekanizma taşıyor,
bu bileşik hücre yok" demişti — sayım doğru, **bileşim yanlıştı**: T5 gerçekten 21 hücre taşıyor
ama vae9182 kolunda `gate:target_logvar` yerine `g2g_kl+adaptive_t` var. Tek tohumlu kol,
tabloda zaten $\dagger$ ile işaretli.

## 4 — Türetilmiş kütüğe giren 5 nicelik

| id | basılı | formül | pay ÷ payda | yeniden hesap |
|---|---|---|---|---|
| `ece_reduction_min` | 41 | yüzde azalma | Stage1 @SWA ECE T=1 → T=1.3406 | 41.4635 → 41 ✓ |
| `ece_reduction_max` | 76 | yüzde azalma | FERPlus @SWA ECE T=1 → T=0.5063 | 76.3748 → 76 ✓ |
| `accuracy_band_widest_arm` | 0.51 | fark | VAE9182 kolu, en yüksek − en düşük @SWA doğruluk | 0.510646 → 0.51 ✓ |
| `selection_cost_best_caption` | 0.52 | fark | dipnottakiyle **aynı** pay/payda, ikinci geçiş | 0.521515 → 0.52 ✓ |
| `tstar_criterion_cost_{min,max}` | 13 / 14 | oran | TS'in kaldırdığı ECE ÷ ölçüt maliyeti | 13.307 / 14.3155 ✓ |

Üç not:

**(a) Yüzde azalmanın paydası cümlede adlandırıldı** — 17 Ağustos kuralı. Payda düzeltilmemiş kol
(T=1), pay ise düzeltilmiş kola göre düşüş. Yeni bir formül sınıfı (`pct_drop`) eklendi; tanımsız
formül zaten ihlal sınıfıydı.

**(b) "0.51 pp" bir aralık değil, EN GENİŞ kol açıklığı.** Ölçülen kol açıklıkları @SWA:
Stage1 0.3042 · VAE9182 **0.5106** · FERPlus 0.4863. §3.6 zaten "0.30 ve 0.51 pp on the two
RAF-DB teachers" diyerek estimandı adlandırıyor; özet üçünün en genişini basıyor.

**(c) §3.2'nin "13–14×"i — geçen tur bulunamamıştı, bugün bulundu.** Operandlar
`tstar_sensitivity.results.<öğretmen>.{ece_removed_by_ts, d_ece}`: FERPlus 0.112593/0.008461 =
**13.31**, primary 13.60, Stage1 0.021980/0.001535 = **14.32**. Basılı iki uç bu iki uca bağlı;
primary aralığın içinde. vae9182 dışarıda çünkü orada TS ECE **ekliyor** (payda negatif) — makale
de "on every teacher where scaling removes ECE at all" diye sınırlıyor. Bu, 14 Ağustos'ta
0.0015/0.0220 diye **basılı yuvarlak değerlerden** yeniden üretilen sayının artık alan yoluyla
duruyor olması demek.

Düzyazı kapsam dışı olmaya devam ediyor; §3.2 ve §5.7 gibi cümleler **tek tek çapa beyanıyla**
içeri alınıyor ve kapsam beyanı bunu söyleyecek biçimde güncellendi.

## 5 — Makale tarafındaki iki düzeltme denetçi tarafından GÖRÜLDÜ

Bu turun en iyi kanıtı, istenmeden geldi: dün bildirilen iki kalem makalede düzeltilince denetçi
**kendiliğinden bağırdı**.

1. `tab_dose_response.stage1.header.T_star_fit` → `rounding_mismatch`: başlık artık 1.35 (fit)
   basıyor, defter ise dağıtılan kola (1.3406 → 1.34) bağlıydı. Bağ fite taşındı.
2. Sıra-istatistiği satırlarının altı bağı → `binding_matched_nothing`: satır etiketi FERPlus'tan
   RAF-DB'ye çevrilmişti. Bağlar yeni etikete taşındı.

**Ve bir tuzak fark edildi.** Başlıkları `tstar_sensitivity.results.*.published_full_nll` ve
`.deployed_T` alanlarına bağlamak üzereydim. O iki alan **elle yazılmış sabit sözlüklerden**
geliyor (`tstar_stability.PUBLISHED`, `tstar_sensitivity.DEPLOYED`): "kampanyada şu değer
yayımlandı/dağıtıldı" **beyanı**, ölçüm değil. Basılı sayıyı oraya bağlamak **dairesel** olurdu —
alan, basılı sayının elle yazılmış kopyası. Bağlar **ölçülen** fitlere kuruldu:
`results.<öğretmen>.T_star_nll` (tam-fold NLL optimumu, n=3068/3153) ve
`tstar_provenance.half_fold_fits.stage1` (yarı-fold fit, 1.3405690 → `1.3406`).

> **Kural olarak yazıyorum:** bağlanabilir bir alan, üreticisinin **hesapladığı** alandır.
> Üreticiye elle yazılmış bir sabit, artefaktta yaşasa bile bağ hedefi değildir.

## 6 — Öz sınama: bugün bağlanan sayılar da geri kondu

11 senaryo, makalenin bir **kopyası** üzerinde (Drive'daki kaynağa dokunulmadan). Taban temiz
kopyada 0 tanım sorunu, 0 kayıtsız.

| senaryo | beklenen sınıf | sonuç |
|---|---|---|
| N13'ün beş makale senaryosu (ECE ×3, doğruluk, 16.3→18.0, §5.7 40×, kayıtsız sayı) | — | **5/5** |
| **§3.2 "13–15 times"** (13–14 yerine) | `printed_not_found_at_location` | **YAKALANDI** |
| **`tab_pooled` Pearson bayat** (+0.930 → +0.931) | `rounding_mismatch` | **YAKALANDI** |
| **`tab_selection_audit` FERPlus satırı bayat** (+0.50 → +0.51) | `rounding_mismatch` | **YAKALANDI** |
| r=0.724 bağı T=0.74 koluna kuruldu (defter bozuldu) | `unresolved_path` | **YAKALANDI** |
| **`T^*` başlığı FIT yerine DAĞITILAN kola bağlandı** (defter bozuldu) | `rounding_mismatch` | **YAKALANDI** |
| bağlı alan artefaktta yok (bayatlık) | `unresolved_path` | **YAKALANDI** |

Sondan ikinci senaryo bu turda eklendi ve 17 Ağustos'ta **gerçekten olan** hatanın kendisi: aynı
artefaktın **komşu alanı**, aynı ad (`T^*`), farklı estimand. Bir bağ ancak bozulduğunda
yakalanabiliyorsa kurulmuştur.

**Yeşil denetçi ancak bu tabloyla birlikte okunabilir.** Tek başına "0 ihlal" hiçbir şey söylemez;
öz sınama, denetçinin bugün bağlanan bağları da gerçekten yokladığını gösteriyor.

## 7 — Kapılar

| kapı | sonuç |
|---|---|
| tablo farkı | **1412/1412 sapma yok** (1364 → 1408 → 1412). 53 sapmanın 5'i MOVED (defterin kendi sayaçları), 48'i APPEARED; **CHANGED/VANISHED yok** |
| Level-1 | **geçti 53** · İHLAL 0 · muaf 9 · başka hata 0 |
| figür | **10/10**, 0 failing |
| mutlak yol | beyansız **0** · okunamayan 0 |
| public kapsam taraması | ihlal **0** |
| `check_numbers` (makaleye karşı) | **çıkış 0** — 0 kayıtsız, 0 uyuşmazlık, 0 kayma |
| `check_numbers_selftest` | **11/11 yakalandı**, çıkış 0 |

Kapıya bu turda üç blok daha girdi, üçü de **makalede basılan ama izlenmeyen** alanlardı:
`two_dataset_overlay.pooled_stats` (12 hücre, yeni Pearson dahil), `noise_units.summary`
(27×/52×/2.6×), `asymmetry_estimand.summary` (1.8–2.0×) ve `tstar_provenance`'ın ölçülen fitleri
(1.35/1.3494/1.3406/0.98). Kayıtsız artefakt korumasız artefakttır.

## 8 — Açık kalemler

1. **İki artefakt aynı $T^*_{\mathrm{NLL}}$'i ayrı ayrı fit ediyor** ve 5. basamakta ayrışıyorlar:
   Stage1 için `p4_teacher_selection` 1.3493927, `tstar_sensitivity` 1.3493829,
   `tstar_provenance` 1.3493927. Basılı hassasiyet (3–4 basamak) ikisini ayırt etmiyor, yani
   **bugün bir hata değil**; ama `tab_selection`'ın `T^*` sütunu p4'e, `tab_dose_response`'un
   başlıkları `tstar_sensitivity`'ye bağlı. Tek kaynağa indirilmeli mi, Fatih'in kararı.
2. **Bağlı 198 hücrenin kaynak artefaktı ihraç bandının DIŞINDA — karar gerekiyor, kendiliğinden
   yapmadım.** Defter kurulunca ölçülebilir hale gelen bir boşluk: bağlanan 19 artefaktın 5'i
   `export_to_drive.py`'nin listesinde yok, ve o beşi **198 basılı hücre** taşıyor.

   | artefakt | hücre | boyut |
   |---|---|---|
   | `p1_dose_response/two_dataset_overlay.json` | 131 | 46 KB |
   | `p4_teacher_selection/p4_teacher_selection.json` | 31 | 10 KB |
   | `ferplus_jsd/ferplus_student_jsd.json` | 29 | 24 KB |
   | `p5_efficiency/latency_benchmark.json` | 6 | 9 KB |
   | `ferplus_jsd/ferplus_jsd.json` | 1 (r=0.724) | 28 KB |

   Sayılar kâğıt tarafında **ulaşılamaz değil** — `number_ledger.json` her hücrenin `exact`
   değerini ve alan yolunu taşıyor ve o ihraç ediliyor. Eksik olan artefaktın kendisi. Bandı
   genişletmek dışa dönük bir karar olduğu için Fatih'e bırakıldı; eklenirse beşi de tek satırlık
   `EXPORTS` girdisi ve MANIFEST kaydı demek.
3. **Kapsam genişletmesi (sıraya alındı, bu turda değil):** supplementary S1–S3 — headroom hükmü
   (0.1126, CI [0.1018, 0.1165]) S2'ye işlendi, artık bağlanabilir; ve `sections/*.tex`
   düzyazısındaki ~470 sayı, revizyon penceresi kapandığında.
4. `noise_units`'in havuz-payda bloğu (`pooled`) makalede **basılmıyor** ama artefaktta duruyor ve
   metnin eski "77×"i oradan geliyordu. Kapıda kayıtlı; makaleye girerse deftere de girmeli.

---

Üretici: `diagnostics/number_ledger.py` · tarayıcı: `diagnostics/paper_number_scan.py` ·
denetçi: `diagnostics/check_numbers.py` · öz sınama: `diagnostics/check_numbers_selftest.py` ·
Pearson kaynağı: `diagnostics/two_dataset_overlay.py`.
Kâğıt ağacı yolu koda yazılmadı: `--paper-root` / `VELD_PAPER_ROOT`; yol verilmezse üretici mevcut
defteri **korur** ve 0 döner.
