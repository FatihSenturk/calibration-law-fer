# Round 3 — A hattı (depo yayını) ve B hattı (tablolama ve yeniden analiz)

> 14 Ağustos 2026 promptu. GPU kuyruğuna hiçbir şey girmedi; her şey önbellek/CPU.
> Sayıların hepsi üreticiden; hiçbiri elle yazılmadı, hiçbiri yuvarlanmış bir tablodan
> geri-hesaplanmadı.

| kapı | sonuç |
|---|---|
| tablo farkı | **1252/1252**, sapma yok (1050 → 1252; 202 yeni hücre, **tamamı APPEARED**) |
| Level-1 | **geçti 50 · İHLAL 0 · muaf 9 · başka hata 0** |
| mutlak yol | 22 dosya beyanlı · **beyansız 0** · okunamayan 0 → GEÇTİ |
| figür | 10/10, 0 failing |
| **kapsam taraması (yeni)** | dört sınıfın dördü de **0** |

---

# A HATTI — Depo yayını

## A1 · `--prune-extras` yazıldı, kapsam taraması koşuldu ✓

İki yeni araç, iki ayrı soru.

**`public_repo_sync.py --prune-extras`.** Betiğin bugüne kadarki bütün soruları **ekleme**
yönündeydi ("hangi dosya girsin"). Bu ilk kez **çıkarma** yönünde soruyor: hedefte duran ama
nihai kapsamda olmayan ne var? Nihai kapsam üç beyanlı kümenin birleşimi — izlenen ∪ onaylı
aday (A+B) ∪ public'e özel — ve dışında kalan her fiziksel dosya fazlalık. `--apply` ile
siler; kapsam kararı burada **verilmez**, `public_scope_buckets.json`'dan okunur.

Ölçüm: **11 fazlalık**. Onu `__pycache__/*.pyc` (zaten `.gitignore` tarafından tutuluyor,
geçmişe hiç girmezdi); **on birincisi gerçek** — `diagnostics/public_scope_scan.py`, yani bu
turda benim yazdığım betik. İlk `--add-approved` koşusunda A kovasındaydı, sonra
`META_PUBLIC`'e alınıp C'ye düştü, ve **fiziksel olarak hedefte kaldı**. Araç kendi
yarattığım durumu yakaladı. Silinmesi **onayınıza bağlı** (dış depodan silme kuralı).

**`diagnostics/public_scope_scan.py` — kapsam taraması.** `public_scope_buckets` "hangi dosya
girsin"i **kaynak** tarafında sorar; bu tarama **hedef** tarafında ve tersinden sorar: diskte
duran ve commit'e girecek olanın **içinde** kapsam-dışı bir sınıf var mı? Kova ataması bir
dosyayı A yapabilir ve o dosyanın içi yine de yayımlanamaz bir şey taşıyabilir.

| # | sınıf | bulgu | nasıl ölçüldü |
|---|---|---|---|
| 1 | **checkpoint** | **0** | commit kümesindeki 48 seri dosyanın (`.pt/.npz/.npy`) hepsi **açıldı**; hiçbirinde parametre tensörü yok |
| 2 | **lisanslı veri türevi** | **0** yeniden dağıtılamaz (4 türev incelendi) | görüntü-adı anahtarı taşıyan her tablo bulundu, hangi veri kümesinden türediği tespit edildi, lisans hükmü beyan olarak yazıldı |
| 3 | **kişisel yol/isim** | 42 alan, **beyanlı** | `abs_path_gate`'in *ikili dosyaları sınamadığını* açıkça yazan boşluğu kapatıldı: `.npz/.npy/.pt` açılıp string alanları **çözülerek** arandı |
| 4 | **ham yüz görüntüsü** | **0** | commit kümesindeki 17 görüntünün hepsi açıldı; hepsi matplotlib üretimi ve hepsi 300 px eşiğinin üstünde |

**Uzantıya güvenilmedi, içerik açıldı** — ve bu fark yaptı: naif kural (`.pt` varsa
checkpoint) bu depoda **yanlış** cevap verirdi. Dört `.pt` dosyasının dördü de logit
önbelleği.

**Tarama gerçek bir kayıp buldu.** O dört `.pt` bucket'ta **A** ama `.gitignore`'un `*.pt`
kuralı onları eliyordu — yani **yayımlanmayacaklardı**. Onları **dokuz üretici** okuyor
(`bootstrap_cis`, `tstar_stability`, `tstar_sensitivity`, `two_dataset_overlay`,
`jsd_sensitivity`, `adaptive_t_headroom_table`, `p1_signed_miscalibration_overlay`,
`ferplus_teacher_signed_grid`, `vote_examples_figure`); onlarsız o tablolar public depoda
öğretmen checkpoint'i olmadan yeniden üretilemezdi — **Level-1 vaadi sessizce delinirdi**.
Toplam 567 KB. `.gitignore`'a gerekçesiyle iki negatif desen eklendi.

Aynı kör nokta bu turda **iki kez daha** çıktı: `ferplus_val_votes10.csv` (B7'nin girdisi)
hiçbir kovaya düşmüyordu, ve `ferplus_abstention_entropy.py` kanonik dosyanın yolunu taşıdığı
için sync tarafından **yazılmayı reddetti**. Üçü de kural boyut/uzantı üzerinden yazılıp
içerik ölçülmediğinde yanlış dosyayı eleme sınıfı.

## A2 · Eksik dosyalar ve commit'siz girişler ✓ — **karar gerektiren kalem yok**

| kalem | adet | hüküm |
|---|---|---|
| kapsam içi olup depoda olmayan | **267** | yazıldı (`--apply --add-approved`), depo toplamı **546** |
| izlenen ama poster-var'da değişmiş | **64** | commit'lenecek — hepsi zaten yayımlanmış dosyaların tazelenmiş hâli |
| kovada **kararsız** (D) | **0** | `ferplus_val_votes10.csv` sınıflandırıldı → A |
| onaylı ama `.gitignore` engelliyordu | 5 | 4 `.pt` açıldı (yukarıda) · `status_heartbeat.md` **bilerek** muaf, `.gitignore`'un kendi içinde tarihli beyanı var |
| fazlalık (kapsamda değil, hedefte duruyor) | 11 | 10'u zaten yok sayılıyor · 1'i silinmeli (**onay bekliyor**) |

**Yanlış pozitif kontrolü.** Kova kuralı izlenen dosyalara uygulandığında **5 dosya C'ye
düşüyor** (`train_affectnetplus_kd.py`, `configs/RAFDB_teacher_affectnet_recipe.yaml`,
`requirements_27may.txt`, `dataset_utils/dataset_{Cifar10,MNIST}.py`). Beşi de **kampanya
adı filtresinin yanlış pozitifi**; beşi de kapsam içi bir dosya tarafından gerçekten
kullanılıyor — ölçüldü: `train_ferplus_kd.py` birincisini sarmalıyor,
`dataset_utils/builder.py` son ikisini **import ediyor**, `tools/build_repro_export.py`
üçüncüsünü okuyor, `METHODS_DATA.md` ikincisini anıyor. Hiçbiri kaçak değil. (`rescue_referenced`
yalnız **adaylara** koşuyor; bu beş dosya kural yazılmadan önce izlenmeye başladığı için o
kurtarmadan hiç geçmemiş.)

## A3 · Dört kapı + commit + public — **iki kararınıza bağlı**

Dört kapı geçti (üstteki tablo) ve README'ye istenen tek satır eklendi:

> *"Which version this is. This snapshot corresponds to the manuscript currently under
> review; the submitted version will be tagged, and a DOI minted from that tag on the day
> of submission."*

**Deponun 404 olmasının sebebi ölçüldü: depo VAR ama PRIVATE.** `gh repo view` →
`{"visibility":"PRIVATE"}`. Uzakta yalnız ilk commit duruyor (`504d8d2`); üç yerel commit
(`5b4f1fa`, `44650f9`, `21737c4`) hiç push edilmemiş. Yani "yayımlama" bir depo kurmak değil,
**görünürlüğü çevirmek**.

Bu iki adım geri alınamaz ve ikisi de sizin kararınız:

1. **11 fazlalığın silinmesi** (10 `.pyc` zararsız, 1 gerçek: `public_scope_scan.py`). Dış
   depodan silme onayınıza bağlı. *Bu commit'ten önce olmalı* — sonra git geçmişinde kalır.
2. **Public'e çevirme.** Prompt bunu istiyor ve gerekçesi sağlam (GfA 95: single-anonymized,
   engel yok). Commit ve push hazır; onay gelince tek turda biter.

---

# B HATTI — Tablolama ve yeniden analiz

Yedi yeni üretici, hepsi kapıya **aynı gün** kaydedildi. Bu turun kendi dersi buydu: N5'te
bayat bir yan tablo haftalarca yaşamıştı çünkü kayıtlı değildi.

## B2 · §5.3'ün iki regresyon doğrusu — **iki ayrı cevap**

`diagnostics/regression_line_provenance.py` → `paper_tables/regression_line_provenance.{md,json}`

**FERPlus 0.582 — üreticisi VAR.** `b015_verdict.py`, havuzlanmış OLS, **@best**, 9 koşu
(3 T kolu × 3 tohum), düz OLS, ağırlıklandırma yok. Tam spesifikasyon:

| checkpoint | eğim | kesme | Pearson | R² (9 koşu) | R² (3 grup ort.) |
|---|---|---|---|---|---|
| @swa ← **makalenin birincil checkpoint'i** | **0.4458** | +0.02464 | +0.8621 | 0.7431 | 0.7534 |
| @best ← **yayımlı** | **0.5824** | +0.02083 | +0.9388 | **0.8814** | **0.8934** |
| @last | 0.5062 | +0.02356 | +0.8894 | 0.7910 | 0.8164 |

Karşı-doğrulayıcının bulamamasının sebebi büyük olasılıkla **checkpoint**: doğru @best'ten
geliyor, makalenin birincil checkpoint'i @swa ve orada aynı fit **0.446** veriyor (%23 fark).
Cümlede checkpoint yazmıyor. R² itirazı da tutmadı: 0.39–0.65 değil, **0.8814 / 0.8934**.

**RAF-DB 0.765 — üreticisi YOK.** Sayı `b015_verdict.py:68`'de bir **sabit**
(`RAFDB_FIT = {"intercept": 0.0244, "slope": 0.7653}`), yalnız veri-kümeleri-arası
karşılaştırma satırında basılıyor, hiçbir betik hesaplamıyor. Defterden **18 spesifikasyon**
tarandı (2 denetim dosyası × 3 checkpoint × 3 sınıf-ağırlığı seçeneği): üretilen eğimler
**1.3522 … 1.8744**, en yakını hedefe **%77** uzak. Pearson +0.992 iddiası taranan
spesifikasyonlarla uyumlu (hepsinde r ≈ 0.99) — yani **anlatı doğru, eğim değil**.

**Hüküm: bu sayının üreticisi yok ve bu veriden çıkmıyor.** Cümlenin işi zaten feragat
olduğu için silinmesi kayıpsız.

## B3 · 18 payda + 17 hücrenin eşleştirilmiş-fark sd'si ✓

`diagnostics/control_sd_mde.py` → `paper_tables/control_sd_mde.{md,json}` ·
`noise_units.py` genişletildi

**18 payda** = 3 öğretmen × 3 checkpoint × 2 eksen, `effective_number` kontrol kolu — 27×
ailesi `logit_std`'den üretiliyor ve o kol bu modda koşuyor. İkinci kontrol kolu (`none`,
gate hücrelerinin paydası) da basıldı: toplam **36 satır**, hangisinin hangi soruya ait olduğu
sütunda yazılı.

**Çapraz kontrol:** aynı paydalarla `noise_units`'in dokuz oranı **yeniden kuruldu** ve
karşılaştırıldı — en büyük sapma **0.00e+00**. Tutmasaydı betik çıkış kodu 1 verirdi.

**17 hücrenin eşleştirilmiş-fark sd'si** zaten `noise_units.md`'deydi ama **yalnız ECE
ekseninde**. Ölçüt doğruluk ekseninde de uygulanıyor (beş öğrenilmiş-sinyal gate hücresine),
dolayısıyla tek eksen basmak o beş hücrenin paydasını okuyucudan saklıyordu. Sayı zaten
JSON'daydı; eksik olan sütundu — tablo artık **her hücre için üç checkpoint × iki eksen**
basıyor.

## B4 · τ×T factorial — **hakemler yanıldı, ve nedeni sergileme** ✓

`diagnostics/tau_t_factorial.py` → `paper_tables/tau_t_factorial.{md,json}`

| τ | T | n | ECE (ort ± sd) | doğruluk (ort ± sd) |
|---|---|---|---|---|
| 3 | 1.70 | 3 | 0.0405 ± 0.0020 | 89.635 ± 0.181 |
| 6 | 0.85 | 3 | 0.0797 ± 0.0016 | 89.646 ± 0.180 |
| 6 | 1.70 | 3 | 0.0447 ± 0.0029 | 89.429 ± 0.196 |
| 12 | 0.85 | 3 | 0.0771 ± 0.0031 | 89.983 ± 0.415 |

**Üç marjinal kontrast — her birinde tek faktör oynuyor, iki kol da defterde mevcut:**

| kontrast | sabit | değişen | ΔECE (ort ± sd) | işaret | Δacc (pp) | işaret |
|---|---|---|---|---|---|---|
| τ etkisi @ T=1.70 | T = 1.70 | τ 3→6 | **+0.0042 ± 0.0028** | `+++` | −0.206 | `--+` |
| τ etkisi @ T=0.85 | T = 0.85 | τ 6→12 | −0.0025 ± 0.0044 | `+--` | +0.337 | `-++` |
| **T etkisi @ τ=6** | τ = 6 | T 0.85→1.70 | **−0.0349 ± 0.0045** | `---` | −0.217 | `---` |

Confound olmadığının kanıtı bu üç satır. Ve sergilenmemiş bir bulgu da veriyorlar: **T'nin
etkisi τ'nunkinin sekiz katından büyük**, ve τ kontrastlarının biri işaret bile tutturmuyor.
DA'nın istediği ayrıştırma (*"varying τ at fixed T … would separate confidence structure
from composite softness"*) tam olarak burada ve sonuç T lehine.

## B5 · Aile 17 değil **22** ✓

`criterion_applied.py`'ye G3.3 bölümü eklendi

| aile | n | aile-bazlı oran |
|---|---|---|
| yalnız ECE ekseni (yayımlı) | 17 | 0.454 |
| **ECE + öğrenilmiş-sinyal doğruluk hücreleri** | **22** | **0.543** ✅ teyit |
| aynı 22 hücre, her biri **kendi** k'siyle | 22 | **0.740** |

(a) 1 − (1 − 0.0350)²² = **0.543** teyit edildi. Ama medyan k ile hesaplamak **eksik
söylüyor**: her hücre kendi k'siyle alındığında oran **0.740**, çünkü oran k'de dışbükey ve
riski birkaç yüksek-k hücresi taşıyor.

(b) **Duyarlılık.** k = σ_eşleştirilmiş/σ_kontrol, 22 hücre üzerinde **0.30 … 3.83**
(medyan 1.70). Hücre-başı oran bu aralıkta **0.0000 → 0.1916** — iki mertebeden fazla
oynuyor. En düşük k `stage1/g2g_kl` (ECE), en yüksek `primary/logit_std` (ECE). *Not:*
promptun andığı 0.33 alt sınırı, aynı hücrenin **yuvarlanmış** değerlerinden (0.0004/0.0012)
çıkıyor; tam hassasiyette 0.30. Üst sınır 2.75 ise `logit_std` hücreleri dahil edilmediğinde
oluşuyor.

(c) **Bağımlılık ölçüldü, varsayılmadı.** Aynı kontrol kolunu paylaşan hücrelerin üç tohum
üzerindeki ortalama ikili korelasyonu **+0.393** (18 çift), paylaşmayanlarda **−0.077**
(213 çift). Ortak bileşen simülasyona konduğunda aile-bazlı oran **0.733**, bağımsızlık
çarpımı ise 0.740 — **fark 0.007**. Yani "bağımsızlık iyimser" uyarısı doğru ama
niceliksel olarak **önemsiz**; artık bu bir varsayım değil ölçüm.

## B6 · Ölçütün minimum saptanabilir etkisi ✓

`paper_tables/control_sd_mde.md` §2

| | @swa | üç checkpoint birlikte |
|---|---|---|
| 2σ (ECE), mutlak | **0.0024 … 0.0067** | 0.0021 … 0.0170 |
| kontrol kolunun kendi ECE düzeyine oran | **%3.2 … %19.4** | %3.2 … %28.0 |

Mutlak taban panelin tahminiyle uyuşuyor (0.0024–0.0066 → tam değer **0.0067**). **Oran
uyuşmuyor:** panel "~%3–9" diyor, ölçülen **%3.2–19.4**. Sebep tek: panel her 2σ'yı
stage1/primary büyüklüğünde bir ECE düzeyine bölmüş, ama vae9182'nin öğrencileri
**0.0278–0.0330**'da oturuyor, yani aynı mutlak taban onun için **%12–19**. Ölçüt vae9182
üzerinde göreli olarak çok daha kör.

`unresolved` bir hücre için doğru cümle: *bu tasarım, o hücrede bu büyüklüğün altındaki bir
etkiyi üç tohumla ayırt edemez.* @best ve @last'te taban daha da yüksek.

## B7 · FERPlus entropisi, abstention kütlesiyle — **cümle yanlış, sonuç sağlam** ✓

`diagnostics/ferplus_abstention_entropy.py` → `paper_tables/ferplus_abstention_entropy.{md,json}`

Kanonik `fer2013new.csv` doğrulama fold'unun **3153 satırının hepsinde** bulundu ve on
sütunun toplamı **her satırda tam 10**. "Eksik oyların sebebi belirlenemez" cümlesi hem
gereksiz hem yanlış: eksik oy diye bir şey yok, oy abstention kategorilerine gitmiş.
Abstention kütlesi taşıyan satır **1176/3153 (%37.3)** — `unknown` 1372 oy, `NF` 58 oy.

| hedef | tanım | ortalama insan entropisi | T\*_JSD | o T'de JSD |
|---|---|---|---|---|
| **(a) koşullu-8** (yayımlı) | sekiz ifadeye yeniden normalize | **0.4401** nat | **0.74** | 0.0440 |
| **(b) abstention-10** | on kategori; öğretmen iki ekstra kategoride 0 | **0.5488** nat | **0.74** | 0.0588 |

(a) yayımlanan `per_sample_human_entropy.npy` ile **birebir** yeniden üretildi (maks sapma
`0.00e+00`). Fark **+0.1087 nat (+%24.7)**.

**T\*_JSD ikisinde de 0.74.** Hizalama sonucu eksik oyların nasıl yorumlandığına **bağlı
değil**; değişen tek şey JSD'nin tabanı ve o taban T'den bağımsız. Yani Ek B'nin katmanlı
kontrolü ayrışmayı zaten koruyordu — düzeltilmesi gereken **yalnız yorum cümlesi**.

*Bu, promptun beklettiği entropi-eşleştirilmiş kolun sıcaklığını da sabitliyor: hedef
0.4401'den sapmadı, dolayısıyla o kol için nişan noktası değişmedi.*

## B8 · Tohum-bazlı doz-yanıt tablosu ✓

`diagnostics/dose_response_per_seed.py` → `paper_tables/dose_response_per_seed.{md,json}`

Üç seri × her T × üç tohum, öğrenci ECE'si — özetin *"all nine seed curves"* cümlesinin ilk
kez bir doğrulama yüzeyi var.

**Monotonluk ekseni burada yeniden tanımlanmadı, G2.2'den ithal edildi** (ikinci bir tanım
yazmak G2.2'nin kapattığı belirsizliği geri açardı): branş içi **9/9**, ham T ekseninde
**0/9**, havuzlanmış |gap|'te 3/9. Bu betiğin kendi T-ekseni ölçümü G2.2 (a) sütunuyla
**birebir tutuyor**: katı monoton 0/9, ρ(T, ECE) > 0 olan 8/9. Çelişki değil — eğriler T\*
civarında U biçimli, sıralama korelasyonu yön verir, monotonluk vermez.

## B9 · `tab_mechanisms`'in üç boş hücresi ✓

`diagnostics/mechanism_grid_gaps.py` → `paper_tables/mechanism_grid_gaps.{md,json}`

Izgara 8 mekanizma × 3 öğretmen = 24 hücre, **21 dolu, 3 boş**:

| öğretmen | mekanizma | hüküm | gerekçe (ölçülen) |
|---|---|---|---|
| stage1 | `g2g_kl+adaptive_t` | **koşulmadı** | defterde bu çift için hiç koşu yok |
| primary | `g2g_kl+adaptive_t` | **koşulmadı** | aynı |
| vae9182 | `gate:target_logvar` | **elendi** | sinyal kalitesi aleyhte: AUROC **0.4579** (şanstan 0.042 uzak), yön *"higher→less error"* — gate'in istediği yön değil. A12 beş hücresini bu taramadan seçti (stage1 0.70, primary 0.84). |

**Dördüncü bir bulgu, boş hücre değil ama aynı sınıf:** `vae9182/g2g_kl+adaptive_t` ızgarada
**n=1** görünüyor, ama defterde aynı kolun **500 epokluk hâli üç tohumla** duruyor — T5'in
bütçe kapısı (400e/SWA@200) onları dışarıda bırakıyor. Doğru cümle *"n=1"* değil,
***"bu bütçede n=1, bir üst bütçede n=3"***.

## B10 · On ondalık uyuşmazlığı ✓

`diagnostics/number_audit_round3.py` → `paper_tables/number_audit_round3.{md,json}`

| # | iddia | yayımlı | panel | **ölçülen** | hüküm |
|---|---|---|---|---|---|
| 1 | "spans 57–77 units" | 57–77 | 57–75.5 | **56.68 – 76.62** | yayımlı doğru |
| 2 | "13–14 times smaller" | 13–14× | 13.2–14.7 | **13.31 – 14.32** | yayımlı doğru |
| 3 | FERPlus best−last SE | 0.0022 | 0.0021 | **0.00215** | **panel haklı** |
| 4 | `tab_efficiency` boyut oranı | 62.9× | 63.1× | **62.8847×** | yayımlı doğru |
| 5 | §5.4 eğim sıralaması | 0.655, 0.716, 0.649 | 0.655, 0.649, 0.716 | kollarla eşleşmeli | aşağıda |
| 6 | baş-izolasyonu "%19" | %19 | %16 | **%18.58** (doğrusal payda) | yayımlı doğru |
| 7 | "37×" vs "~40×" | ikisi de | hangisi? | **açıklık 0.020083 → 39.8×** | ~40× doğru |
| 8 | FERPlus T\*_ECE | 0.453 / 0.46 / ≈0.46–0.51 | kesin değer? | **0.45305** | üçü üç farklı şey |
| 9 | Ek B yoğun grid alt sınırı | 0.5 | sınır-kısıtlı mı? | **hiçbir ızgaraya ait değil** | sınır-kısıtlı **değil** |
| 10 | "bar" terimi | 1× / 2× | hangisi nerede? | **bar = 1× sd, eşik = 2×bar** | ikisi de doğru, farklı birim |

Ayrıntılar:

- **(4)** Payda öğrencinin gerçek boyutu **8.826 MB**, 8.8 değil: 555.0155/8.8259 = **62.885**.
  Panelin 63.1'i paydayı yuvarlamaktan geliyor.
- **(5)** Sayılar doğru; **sıra kol adıyla yazılmadıkça belirsiz**. Kollar: `scratch0712`
  = **0.655**, `pretrained2248` = **0.716**, `scratch2248` = **0.649**. Kontrastlar:
  başlatma **−0.067** (çözünüyor), kapasite **−0.006** (çözünmüyor), ikisi birden **+0.061**
  (çözünmüyor). Toplamsal olarak tutarlı: (+0.067) + (−0.006) = +0.061.
- **(6)** Δ = 0.00622; **doğrusal** öğrencinin ECE'si 0.03348 → **%18.58** (≈19).
  Varyasyonel öğrencininki 0.02725 → %22.82. Panelin %16'sı hiçbir paydadan çıkmıyor.
  **Ayrıca:** bu kol **@best**'te ölçülmüş, birincil checkpoint @swa.
- **(7)** Öğrenci JSD açıklığı @swa = 0.073681 − 0.053598 = **0.020083**. Tohum sd'si
  konvansiyona göre: ortalama 0.000505 → **39.8×** · medyan 0.000456 → 44.1× · en büyük
  0.000733 → 27.4× · en küçük 0.000373 → 53.9×. **37× hiçbirinden çıkmıyor.**
- **(8)** **0.4530** = sürekli argmin (sınırlı Brent). **0.46** = aynı optimumun kaba
  ızgaradaki hâli (adım 0.02, 196 nokta). **0.5063** ise T\*_ECE **değil**, T\*_NLL —
  dağıtılan sıcaklık. Metindeki "≈0.46–0.51" iki farklı ölçütü tek aralık gibi gösteriyor.
- **(9)** **0.5 hiçbir ızgaranın sınırı değil.** İki ayrı ızgara var: RAF-DB **[0.60, 2.95]**
  adım 0.05 (48 nokta), FERPlus **[0.10, 4.00]** adım 0.02 (196 nokta), sürekli fit
  [0.1, 3.0] adım 0.005. Hiçbir optimum sınırda değil (RAF-DB 1.350/1.240/1.060, FERPlus
  0.46) → **headroom aralığı sınır-kısıtlı değil**; düzeltilmesi gereken tek şey Ek B'nin
  ızgarayı tek sayıyla anması.
- **(10)** `p6_verdict.py` "bar"ı **1× kontrol tohum sd'si** diye tanımlıyor (0.0012) ve
  hükmü `|ort| ≥ 2×bar` ile veriyor. Tanım tek; `tab_collapse` 2×bar'a bölüyor (eşiğin kaç
  katı), §5.5 1×bar'a bölüyor (gürültünün kaç katı). İkisi de meşru ama **tam iki katı**;
  metin hangi birimi kullandığını her iki yerde de yazmalı.

---

## Makale tarafına düşen özet

| madde | eylem |
|---|---|
| B2 | RAF-DB'nin 0.765'i **silinsin** (üreticisi yok). FERPlus 0.582'ye **"@best"** etiketi eklensin, ya da birincil checkpoint'e (0.446 @swa) geçilsin. |
| B3/B6 | 18 payda ve 2σ tabanı ek tabloya girsin; `unresolved` cümlesi tabanla birlikte yazılsın. Panelin "%3–9"u **%3.2–19.4** olmalı. |
| B4 | Kurucu (τ, T) değerleri ve üç marjinal kontrast §5.5'e girsin — hakem itirazının doğrudan cevabı. |
| B5 | Aile **22**, oran **0.543**; kendi-k'sıyla 0.740 da anılsın. Bağımlılık düzeltmesi 0.007, ölçüldü. |
| B7 | "Eksik oyların sebebi belirlenemez" cümlesi **çıkarılsın**; yerine koşullu-hedef açıklaması + %37.3 abstention. T\*_JSD değişmiyor. |
| B8 | Ek tablo eklensin; "monotone" cümlesi G2.2'nin ekseniyle (branş içi) etiketlensin. |
| B9 | Üç boş hücre tek satırla açıklansın; `vae9182/g2g_kl+adaptive_t` için "bu bütçede n=1". |
| B10 | 3, 5, 8, 9, 10 düzeltilsin; 1, 2, 4, 6, 7 olduğu gibi kalsın. |

---

Üretici: elle yazıldı · ölçümler `public_scope_scan`, `public_repo_sync --prune-extras`,
`control_sd_mde`, `tau_t_factorial`, `mechanism_grid_gaps`, `dose_response_per_seed`,
`ferplus_abstention_entropy`, `regression_line_provenance`, `number_audit_round3`,
`criterion_applied` (G3.3), `noise_units` çıktıları üzerinden
