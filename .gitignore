# Never commit these. Datasets and checkpoints are not redistributable (see README),
# run directories and logs are large and are not needed to regenerate any table.
checkpoints/
data/
dataset_cache/
kd_logs*/
launcher_logs/
pipeline_logs/
pretrained/
reference_90_74/
results/
swanlog/
wandb/
__pycache__/
*.pyc
*.pt
*.pth
*.pth.tar
*.tar
models/mobilefacenet_model_best.pth.tar
*.log
*.csv.bak

# Figure outputs. Deliberately untracked: they are regenerated by
# diagnostics/export_paper_figures.py and gated by diagnostics/verify_paper_figures.py,
# so the guarantee lives in the gate rather than in a committed binary.
paper/
*.pdf

# Pinned to the ROOT level: as a bare `reports/` pattern this also swallowed
# `diagnostics/reports/`, i.e. the laboratory records the repository is meant to carry.
# The output folder at the root was always what was intended.
/reports/
