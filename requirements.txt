# Versions this campaign actually ran on (Python 3.13.10, Windows 11, CUDA 12.8).
# The analysis layer -- everything that regenerates a table in the paper -- needs
# only the second block and runs on CPU.

# --- training (GPU) ---
torch==2.10.0
torchvision==0.25.0
timm==1.0.24
opencv-python
pillow
tqdm

# --- analysis / tables / figures (CPU only) ---
numpy
scipy==1.16.3
pandas==2.3.3
scikit-learn==1.8.0
matplotlib==3.10.8
PyYAML
thop            # FLOPs/MACs counting; note kd_common.measure_flops returns MACs
PyMuPDF==1.27.1 # figure gate: reads back the produced PDFs (verify_paper_figures.py)

# --- experiment tracking, optional ---
# Training scripts log to these when available; they are not needed to
# reproduce any table and can be omitted.
# swanlab
# wandb
# tensorboard
